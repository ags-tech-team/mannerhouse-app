{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "docker-compose.yml"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "docker-compose up -d",
    "healthcheckPath": "/api/health"
  }
}